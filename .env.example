# Copiar a .env y completar valores locales. .env nunca se commitea.

# Ruta a la base de datos SQLite (relativa a la raíz del repo)
DATABASE_PATH=data/db/oprc.sqlite

# URL pública del sitio (usada en metadata y en el User-Agent de los scrapers)
SITE_URL=https://www.oprcoquimbo.cl

# --- Fase 5: Newsletter (dejar vacío hasta implementar) ---
RESEND_API_KEY=
